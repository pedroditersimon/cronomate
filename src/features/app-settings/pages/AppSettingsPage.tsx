import AppSettings from "src/features/app-settings/components/AppSettings";
import PageLayout from "src/shared/layouts/PageLayout";

export default function AppSettingsPage() {
    return (
        <PageLayout>
            <AppSettings>

            </AppSettings>
        </PageLayout>
    );
}